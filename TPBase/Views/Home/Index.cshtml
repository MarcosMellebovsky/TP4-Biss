@{
    ViewData["Title"] = "Index";
}

<h1>mi pagina de paises</h1>
@foreach (var item in ViewBag.ListaPaises)
{
    <a href='@Url.Action("DetallePais", new {Pais = item.Nombre})'><img src = "/img/@item.imgBandera"></a>
}